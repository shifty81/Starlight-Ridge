Starlight Ridge Phase 34 - Web Asset Lab Professional UI Polish

Apply over project root after Phase 33 and overwrite files.

Purpose:
- Keep browser editor as the UX prototype/source of truth.
- Polish the Asset Lab center pixel editor layout.
- Move core tools to a left vertical icon rail.
- Move color/RGBA/display controls to the right preview/properties panel.
- Keep the center canvas focused on editing.
- Keep seam cleanup/export actions in a bottom action bar.
- Make grid/display toggles much more visible.

Run:
  RUN_ASSET_LAB.bat

No Rust files are changed. Cargo check is not required for this patch.
