# Native Asset Studio Phase 32 Preview Helpers

Phase 32 restores the active native preview-card drawing helpers used by the Asset Studio inspector.

The right inspector preview cards should now build again for:

- Tile preview
- Seam diagnostic preview
- Atlas miniature preview

This patch is intentionally narrow and does not add new editor features beyond restoring the missing helper functions.
